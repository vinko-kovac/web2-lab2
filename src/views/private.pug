html 
    head 
        title Web2 Lab1
    body
        h1 User data:
        label Userinfo: #{user}
        if t
            form(action="/unsecureLogout", method="POST")
                input(type="submit", value="Logout")
        else 
            form(action="/secureLogout", method="POST")
                input(type="submit", value="Logout")